# ===========================================
# Backend Environment (Dev against real infra)
# ===========================================
# Run backend locally but connect to real Fly VMs and Supabase

# Server
API_PORT=8080

# Use real Fly.io VMs (not local Docker containers)
LOCAL_MODE=false

# Production Supabase (update these with your real values)
DATABASE_URL=postgresql://postgres.ixamqnvlxlgezjtvbgpf:aether123456@aws-0-us-west-1.pooler.supabase.com:6543/postgres
SUPABASE_URL=https://ixamqnvlxlgezjtvbgpf.supabase.co
# Get these from Supabase dashboard -> Settings -> API
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_SECRET=

# Fly.io
FLY_API_TOKEN=
FLY_VMS_APP_NAME=aether-vms
FLY_REGION=sjc
BASE_IMAGE=registry.fly.io/aether-vms/base:latest

# SSH (same key used for Fly VMs)
SSH_PRIVATE_KEY_PATH=./.keys/aether_rsa

# Encryption (use same key as production for API key decryption)
ENCRYPTION_MASTER_KEY=
