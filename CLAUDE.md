Never use fallbacks. Always fail loudly.

When using integrations like Fly.io and Supabase, ensure to use abstractions and build in a way that makes it easy to transition to AWS or another cloud provider later.